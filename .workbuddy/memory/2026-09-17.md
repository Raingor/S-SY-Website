# 2026-09-17

## 读取 pi 会话 01a09d91-68e8-729c-b95e-9d9ad08b0815（S-SY-Website 主会话）

会话跨度 09-14 ~ 09-17，285 条消息。要点：

- 项目 = 希腊私人定制旅行官网 + 后台；React 19 + Vite + Node ESM；后台路径 `/manage-9f3k7`。
- 生产 https://www.sy-greece.com/ ，服务器 `root@106.15.53.150`（密钥 `/Users/mac-2312-r/workspace/wwwroot/M-projects/S-SY/sy-secret.pem`），systemd `sy-greece.service`，环境文件 `/etc/sy-greece.env`。
- **存储分叉**：生产 = MariaDB（`sygreece_website.sy_site_data`，data_key=`main` 单条 payload_json）；本地也已切成 MariaDB（库同名，账号 `sygreece_web@127.0.0.1`，root 密码 root2312），`.env` 配置、已 gitignore。`data/site-data.json` 只是种子文件且含生产 PII，**不要入库**。
- **部署流程**：不是普通 git push。本机 `git bundle create` → scp 到 `/tmp/sy-greece-<commit>.bundle` → 服务器 `git fetch <bundle> HEAD:branch` + `git merge`（`git merge <bundle>` 直接跑会失败，`bundle pull` 该 Git 版本不支持）→ `systemctl restart sy-greece.service`。生产无 npm，靠 bundle 里的构建产物 `index.html`。
- **坑**：`storage.mjs` 在模块加载时就读 `SY_STORAGE`，所以 `.env` 加载器必须放 `storage.mjs` 顶部（放 server.mjs 无效）。
- **坑**：后台桌面端是固定 100vh + 内部滚动（1253–1283 段 CSS），改 `.admin-content` 宽度容易误删该段导致页面贴底不能滚。
- 当前 HEAD = `c7900b8`（小程序分享字段 + 目的地关联景点），**尚未 push**（origin/main 停在 `de5a3fc`）。
- 待办：15 景点 / 8 目的地的 `shareTitle`、`shareImage`、`attractionId` 仍为空，需主仓确认后配。

## 18:15 部署 c7900b8 到生产

- push `main` → `de5a3fc..c7900b8`（GitHub Raingor/S-SY-Website）
- 改用**增量 bundle**（`git bundle create f de5a3fc..HEAD`）→ 2.5KB，替代之前 57MB 全量 bundle
- 服务器 fetch + merge 干净，新 HEAD `b518f018c`，重启 `sy-greece.service`
- 验收：health/home/admin/线上首页全部 200；`/api/content` 输出 `shareTitle` 全量就位、`fullImage` 单 images 正确、无双 images
- **发现的小契约瑕疵**：`shareImage` 为空时 `imageUrl(undefined)` 返回 undefined → JSON 直接丢键，attraction 上没有 `shareImage` 字段（契约写的是"可空"）。MpApp 取 undefined 会走回退，行为一致，暂未改。

## 18:30 后台 PostHog 主题改造（DESIGN.md，方向 A，未提交）

- 做法：**不重写原 admin 区块**，在 `src/styles.css` 末尾追加作用域主题块（`.admin-shell/.admin-login/.admin-toast` 内重映射 CSS 变量 + 硬编码色覆盖），公开站不受影响。`src-entry.html` 加 IBM Plex Sans 字体链接。
- 关键令牌：画布 `#eeefe9`、卡片 `#fff`+hairline `#bfc1b7`、ink `#23251d`、主 CTA `#f7a501`（按压 `#dd9001`/激活 `#b17816`）、链接/图标 `#1d4ed8`、语义 pastel（蓝 `#dceaf6`/绿 `#d9eddf`/红 `#f7d6d3`/紫 `#e7d8ee`）、圆角 4/6/8、零阴影。侧边栏从深海军蓝改为奶油 soft 面。
- Chrome 实测：总览/路线管理/站点配置三页截图确认；`--blue` 等变量重映射生效；settings 面板可滚（maxScroll>0）、radius 6px、shadow none。
- 状态：**已提交并 push，未部署**。commit `1376aed`（`c7900b8..1376aed`），origin/main 同步。本地服务后台运行中（npm run server, :4173）。
- 生产仍是 `b518f018c`（= c7900b8 的内容），**后台主题还没上线**。要上线时打增量 bundle：`git bundle create /tmp/sy-greece-1376aed.bundle c7900b8..1376aed`。
- 注意：`.workbuddy/` 未加入 .gitignore，是 untracked 状态，提交时别 `git add -A`。
