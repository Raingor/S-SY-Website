# 2026-09-18

## 更新网页端设计文档与设计稿（S-SY 主仓）

叶荣要求：`S-SY/sy-greece-website`（设计稿目录）+ `SY希腊蔚蓝海岸-网页端设计文档.md` 按当前网站实现同步更新。

### 设计文档 v2.1 → v2.2（已重写）

- 新增「v2.1 → v2.2 变更摘要」表；版本行 v2.2 · 2026-09-18。
- 补充实际实现：多语言切换器（简/繁/英三语开放，日/希文案内置未开放；`?lang=` + localStorage）、导航 7 项、全局咨询浮窗 ConsultationDock、奢享体验暗区板块、定制行程横幅、路线卡改「参考行程 · 免费浏览」跳 `/itineraries/:id`。
- 新页面章节：P11 景点导览（15 景点 / 6 城市，`/attractions` 三层路由）、P12 参考行程索引 + 专属行程页 `/trip/:token`（noindex）、P13 合规组件化。
- 新章节：SEO 与结构化数据（JSON-LD @graph / canonical / noindex 策略 / WebP）；管理后台设计（PostHog 令牌表 + 布局 + 三组导航 + 锚点编辑器）。
- 占位核对：Jenny 微信 SYGJ1130 / 电话 +86 150 7146 5661 已真实上线（customize 页）；页脚三项目、ICP、知识库支付、预约日历仍占位。
- 视觉基准声明：以 `sy-greece-website/16-当前实现-2026-09-18/` 截图为准，01–15 手绘稿降级为参考。

### 设计稿更新方式

- 未重绘手绘稿（不现实也没必要），改为**全页真实截图**：playwright-core + 本机 Chrome（channel: 'chrome'），Desktop 1440×900 / Mobile 375×812，deviceScaleFactor 2，输出 PNG 到 `S-SY/sy-greece-website/16-当前实现-2026-09-18/`。
- Desktop 14 张（含后台总览 + 内容编辑，自动登录 `/manage-9f3k7`）+ Mobile 8 张。
- 脚本：`/Users/mac-2312-r/.workbuddy/binaries/node/workspace/scripts/sy-shots.mjs`（NODE_PATH 指向 workspace/node_modules，playwright-core 1.55.0）。

### 坑

- **chrome-devtools MCP 截图 filePath 全部被拒**（workspace roots 不含本机任意路径，/tmp 也不行）→ 换 playwright-core 自写脚本。
- **playwright-cli 插件坏**：`Cannot find module 'playwright/lib/mcp/terminal/program'`，不可用。
- **npm install 在空目录报 "must provide string spec"**：先 `printf '{...}' > package.json` 再装就好了。
- **懒加载图片**：fullPage 截图前必须 `img.loading='eager'` + 滚动 + 等 `decode()`，否则下半页卡片灰底。
- MCP 的 take_screenshot 只能回传图片，无法落盘；playwright 的 screenshot path 不受 workspace root 限制。

### 完成状态（10:45）

- 22 张截图（Desktop 14 + Mobile 8）已就位 `S-SY/sy-greece-website/16-当前实现-2026-09-18/`，共 82MB。抽查首页（目的地图全齐）、后台编辑页（PostHog 主题 + 锚点导航）、移动端首页均正常。
- 设计文档已重写为 v2.2。
- **根因补充**：本地灰图不是懒加载，是 server.mjs 静态服务只认 `dist/` 目录（`filePath = join(distDir, safePath)`），destination-*.jpg 在本地不存在 → SPA fallback 返回 index.html（200 + text/html）。修复 = scp 生产图片（14MB/26 个）→ public/images + dist/images 双份。生产图片 rsync 拉取（只读），符合「图片资源可 rsync」约定。
- **注意**：`public/images/` 新增 10 个 destination-*/guide-profile-* 生产图（未跟踪）；git status 里 S-SY 主仓还有别的会话留下的小程序文档/PNG 改动，提交时要分开。S-SY 主仓未提交，等叶荣确认（82MB 截图入库会明显增重）。
